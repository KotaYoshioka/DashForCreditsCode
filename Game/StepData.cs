using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StepData : MonoBehaviour
{
    public static Vector2 GetStepLocation(int number,int step,bool up)
    {
        switch (number)
        {
            case 3:
                switch (step)
                {
                    case 2:
                        break;
                }
                break;
        }
        return Vector2.zero;
    }
}
